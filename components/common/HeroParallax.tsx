"use client";
import React from "react";
import {
  motion,
  useScroll,
  useTransform,
  useSpring,
  MotionValue,
} from "framer-motion";
import Image from "next/image";
const HeroParallax = ({ title}: { title: string }) => {
  return <div>HeroParallax</div>;
};

export default HeroParallax;
